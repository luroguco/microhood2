<h1>Listing ideas</h1>

<table>
  <tr>
    <th>Description</th>
    <th>Rating</th>
    <th>Neighborhood</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ideas.each do |idea| %>
  <tr>
    <td><%= idea.description %></td>
    <td><%= idea.rating %></td>
    <td><%= idea.neighborhood %></td>
    <td><%= link_to 'Show', idea %></td>
    <td><%= link_to 'Edit', edit_idea_path(idea) %></td>
    <td><%= link_to 'Destroy', idea, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Idea', new_idea_path %>
